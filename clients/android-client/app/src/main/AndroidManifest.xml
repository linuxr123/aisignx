<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <!-- Allows PackageInstaller to install our own APK during silent updates.
         On Android 8+ this requires the user to grant "Install unknown apps"
         once for AISignX Player; on Device Owner kiosks the install is fully
         silent with no prompt. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android TV (Sony Bravia, NVIDIA Shield, etc.) support.
         leanback=optional means we run on phones AND TVs.
         touchscreen=optional means TVs don't reject the install. -->
    <uses-feature android:name="android.software.leanback"  android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:name=".SignageApp"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:banner="@drawable/ic_banner"
        android:label="@string/app_name"
        android:theme="@style/Theme.AISignXPlayer"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Setup screen — shown when no server/token is configured -->
        <activity
            android:name=".ui.SetupActivity"
            android:exported="true"
            android:screenOrientation="fullSensor"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Waiting-for-approval screen -->
        <activity
            android:name=".ui.WaitingActivity"
            android:exported="false"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|uiMode"
            android:screenOrientation="fullSensor" />

        <!-- Fullscreen kiosk player -->
        <activity
            android:name=".ui.PlayerActivity"
            android:exported="false"
            android:screenOrientation="fullSensor"
            android:launchMode="singleTask"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|uiMode" />

        <!-- Boot receiver — restarts player after device reboot -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Install-result receiver — handles PackageInstaller commit callbacks
             during silent self-updates. Auto-relaunches the app on success. -->
        <receiver
            android:name=".UpdateInstallReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.aisignx.player.INSTALL_RESULT" />
            </intent-filter>
        </receiver>

        <!-- Device Owner admin receiver. Optional, but required for fully
             unattended Android kiosk updates with PackageInstaller. -->
        <receiver
            android:name=".AisignxDeviceAdminReceiver"
            android:description="@string/app_name"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
