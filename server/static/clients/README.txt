Place built client packages here after running build scripts from the repo root:

  AISignX-Player-Setup.exe   — Windows installer (Electron)
  AISignX-Player.AppImage    — Linux portable (Electron)
  AISignX-Player.deb         — Linux Debian package (Electron)
  AISignX-Player.apk         — Android APK

Build: see ../../clients/README.md and build_clients_*.ps1 / .sh at repo root.
Installers are gitignored; only client_versions.json is tracked as a template.
